1) Crea un saludador personalizable. Consiste en un simple Frame con un campo de
texto (TextField) y un botón (Button). Cuando pulsemos el botón, aparecerá un
mensaje emergente (MessageBox) con el texto “¡Hola <texto escrito en el campo
de texto>!”.
